extends: state/combat.yaml
fields:
  run: run
  map: map
  reward: reward
  selection: selection
  chest: chest
  event:
    transform: event
  shop:
    transform: shop
  rest: rest
  character_select: character_select
  modal: modal
  game_over:
    transform: game_over
